[build]
  command = "npm run build"
  publish = "dist/public"
  functions = "netlify/functions"

[context.production.environment]
  NODE_ENV = "production"

# Optional: redirect /api/* to another backend if you host the API elsewhere
# [[redirects]]
#   from = "/api/*"
#   to = "https://your-api.example.com/:splat"
#   status = 200